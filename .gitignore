# Build output
bin/
obj/
TestResults/
publish/
*.binlog

# IDE / user state
.vs/
*.user
.idea/

# Claude Code local (per-machine permission grants)
.claude/settings.local.json

# SimConnect SDK DLLs are never committed — they are Microsoft SDK components and
# not redistributable. This covers the vendored copies in libs/SimConnect/ too:
# that directory is a local build fallback only (see libs/SimConnect/README.md).
SimConnect.dll
Microsoft.FlightSimulator.SimConnect.dll
